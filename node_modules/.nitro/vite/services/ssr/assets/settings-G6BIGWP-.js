import { t as supabase } from "./client-x1NRwdI2.js";
import { S as useSettings, b as useSaveSettings, d as useCategories, f as useChickenTypes, g as useProfile, j as prettyTime, l as useAuditLogs, m as useMyRoles, r as useAddCategory, s as useAddSupplier, w as useSuppliers } from "./data-BXdCoojx.js";
import { n as PageHeader } from "./Stat-C4Ts9jut.js";
import { t as Button } from "./button-BkEeRci-.js";
import { t as Input } from "./input-B8Q2ztVi.js";
import { t as Label } from "./label-DBD1bRRP.js";
import { useEffect, useState } from "react";
import { useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
import { LogOut } from "lucide-react";
//#region src/routes/_authenticated/settings.tsx?tsr-split=component
function SettingsPage() {
	const navigate = useNavigate();
	const settings = useSettings();
	const save = useSaveSettings();
	const profile = useProfile();
	const roles = useMyRoles();
	const suppliers = useSuppliers();
	const addSupplier = useAddSupplier();
	const categories = useCategories();
	const addCategory = useAddCategory();
	const types = useChickenTypes();
	const logs = useAuditLogs(40);
	const [form, setForm] = useState({
		business_name: "",
		phone: "",
		address: "",
		stock_tolerance_kg: "",
		target_margin_per_kg: "",
		variable_cost_per_kg: ""
	});
	const [newSupplier, setNewSupplier] = useState("");
	const [newCategory, setNewCategory] = useState("");
	const [newCategoryKind, setNewCategoryKind] = useState("operating");
	useEffect(() => {
		const s = settings.data;
		if (!s) return;
		setForm({
			business_name: s.business_name ?? "",
			phone: s.phone ?? "",
			address: s.address ?? "",
			stock_tolerance_kg: String(s.stock_tolerance_kg ?? 0),
			target_margin_per_kg: String(s.target_margin_per_kg ?? 0),
			variable_cost_per_kg: String(s.variable_cost_per_kg ?? 0)
		});
	}, [settings.data]);
	async function signOut() {
		await supabase.auth.signOut();
		navigate({ to: "/auth" });
	}
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx(PageHeader, {
			title: "Settings",
			description: profile.data?.full_name ? `Signed in as ${profile.data.full_name}` : "Shop configuration",
			action: /* @__PURE__ */ jsxs(Button, {
				variant: "outline",
				className: "h-11",
				onClick: signOut,
				children: [/* @__PURE__ */ jsx(LogOut, { className: "size-4" }), "Sign out"]
			})
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "grid gap-4 lg:grid-cols-2",
			children: [/* @__PURE__ */ jsxs("section", {
				className: "rounded-xl border bg-card p-4 shadow-[var(--shadow-card)]",
				children: [
					/* @__PURE__ */ jsx("h2", {
						className: "text-sm font-semibold",
						children: "Shop details"
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "mt-3 grid gap-3 sm:grid-cols-2",
						children: [
							/* @__PURE__ */ jsx(Field, {
								label: "Business name",
								value: form.business_name,
								onChange: (v) => setForm({
									...form,
									business_name: v
								})
							}),
							/* @__PURE__ */ jsx(Field, {
								label: "Phone",
								value: form.phone,
								onChange: (v) => setForm({
									...form,
									phone: v
								})
							}),
							/* @__PURE__ */ jsx("div", {
								className: "sm:col-span-2",
								children: /* @__PURE__ */ jsx(Field, {
									label: "Address",
									value: form.address,
									onChange: (v) => setForm({
										...form,
										address: v
									})
								})
							}),
							/* @__PURE__ */ jsx(Field, {
								label: "Stock tolerance (kg)",
								value: form.stock_tolerance_kg,
								onChange: (v) => setForm({
									...form,
									stock_tolerance_kg: v
								}),
								numeric: true
							}),
							/* @__PURE__ */ jsx(Field, {
								label: "Target margin (₹/kg)",
								value: form.target_margin_per_kg,
								onChange: (v) => setForm({
									...form,
									target_margin_per_kg: v
								}),
								numeric: true
							}),
							/* @__PURE__ */ jsx(Field, {
								label: "Variable cost (₹/kg)",
								value: form.variable_cost_per_kg,
								onChange: (v) => setForm({
									...form,
									variable_cost_per_kg: v
								}),
								numeric: true
							})
						]
					}),
					/* @__PURE__ */ jsx(Button, {
						className: "mt-4 h-11 w-full",
						disabled: save.isPending,
						onClick: () => save.mutate({
							business_name: form.business_name || "Brothers Chicken Adda",
							phone: form.phone || null,
							address: form.address || null,
							stock_tolerance_kg: Number(form.stock_tolerance_kg || 0),
							target_margin_per_kg: Number(form.target_margin_per_kg || 0),
							variable_cost_per_kg: Number(form.variable_cost_per_kg || 0)
						}, {
							onSuccess: () => toast.success("Settings saved"),
							onError: (e) => toast.error(e.message || "Could not save")
						}),
						children: "Save settings"
					}),
					/* @__PURE__ */ jsxs("p", {
						className: "mt-3 text-xs text-muted-foreground",
						children: [
							"Access is private: ",
							roles.data?.length ? roles.data.join(", ") : "member",
							" · every change is written to the audit trail and the database is backed up automatically."
						]
					})
				]
			}), /* @__PURE__ */ jsxs("section", {
				className: "rounded-xl border bg-card p-4 shadow-[var(--shadow-card)]",
				children: [
					/* @__PURE__ */ jsx("h2", {
						className: "text-sm font-semibold",
						children: "Suppliers"
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "mt-3 flex gap-2",
						children: [/* @__PURE__ */ jsx(Input, {
							className: "h-11",
							placeholder: "New supplier name",
							value: newSupplier,
							onChange: (e) => setNewSupplier(e.target.value)
						}), /* @__PURE__ */ jsx(Button, {
							className: "h-11",
							disabled: !newSupplier.trim() || addSupplier.isPending,
							onClick: () => addSupplier.mutate({ name: newSupplier.trim() }, {
								onSuccess: () => {
									setNewSupplier("");
									toast.success("Supplier added");
								},
								onError: (e) => toast.error(e.message)
							}),
							children: "Add"
						})]
					}),
					/* @__PURE__ */ jsxs("ul", {
						className: "mt-3 space-y-1 text-sm",
						children: [(suppliers.data ?? []).map((s) => /* @__PURE__ */ jsx("li", {
							className: "rounded-lg bg-muted/50 px-3 py-2",
							children: s.name
						}, s.id)), (suppliers.data ?? []).length === 0 ? /* @__PURE__ */ jsx("li", {
							className: "text-sm text-muted-foreground",
							children: "No suppliers yet."
						}) : null]
					}),
					/* @__PURE__ */ jsx("h2", {
						className: "mt-6 text-sm font-semibold",
						children: "Expense categories"
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "mt-3 flex gap-2",
						children: [
							/* @__PURE__ */ jsx(Input, {
								className: "h-11",
								placeholder: "New category",
								value: newCategory,
								onChange: (e) => setNewCategory(e.target.value)
							}),
							/* @__PURE__ */ jsxs("select", {
								className: "h-11 rounded-md border bg-background px-2 text-sm",
								value: newCategoryKind,
								onChange: (e) => setNewCategoryKind(e.target.value),
								children: [/* @__PURE__ */ jsx("option", {
									value: "operating",
									children: "Operating"
								}), /* @__PURE__ */ jsx("option", {
									value: "direct",
									children: "Direct"
								})]
							}),
							/* @__PURE__ */ jsx(Button, {
								className: "h-11",
								disabled: !newCategory.trim() || addCategory.isPending,
								onClick: () => addCategory.mutate({
									name: newCategory.trim(),
									kind: newCategoryKind
								}, {
									onSuccess: () => {
										setNewCategory("");
										toast.success("Category added");
									},
									onError: (e) => toast.error(e.message)
								}),
								children: "Add"
							})
						]
					}),
					/* @__PURE__ */ jsx("div", {
						className: "mt-3 flex flex-wrap gap-2 text-sm",
						children: (categories.data ?? []).map((c) => /* @__PURE__ */ jsx("span", {
							className: "rounded-full bg-muted px-3 py-1",
							children: c.name
						}, c.id))
					}),
					/* @__PURE__ */ jsx("h2", {
						className: "mt-6 text-sm font-semibold",
						children: "Chicken types"
					}),
					/* @__PURE__ */ jsx("div", {
						className: "mt-3 flex flex-wrap gap-2 text-sm",
						children: (types.data ?? []).map((t) => /* @__PURE__ */ jsx("span", {
							className: "rounded-full bg-muted px-3 py-1",
							children: t.name
						}, t.id))
					})
				]
			})]
		}),
		/* @__PURE__ */ jsxs("section", {
			className: "mt-4 overflow-hidden rounded-xl border bg-card shadow-[var(--shadow-card)]",
			children: [/* @__PURE__ */ jsx("h2", {
				className: "border-b px-4 py-3 text-sm font-semibold",
				children: "Audit trail"
			}), (logs.data ?? []).length === 0 ? /* @__PURE__ */ jsx("p", {
				className: "px-4 py-8 text-center text-sm text-muted-foreground",
				children: "Nothing recorded yet."
			}) : /* @__PURE__ */ jsx("div", {
				className: "max-h-80 overflow-y-auto",
				children: (logs.data ?? []).map((l) => /* @__PURE__ */ jsxs("div", {
					className: "flex justify-between gap-3 border-b px-4 py-2 text-sm last:border-0",
					children: [/* @__PURE__ */ jsx("span", {
						className: "font-medium",
						children: l.action
					}), /* @__PURE__ */ jsx("span", {
						className: "text-xs text-muted-foreground",
						children: prettyTime(l.created_at)
					})]
				}, l.id))
			})]
		})
	] });
}
function Field({ label, value, onChange, numeric }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-1.5",
		children: [/* @__PURE__ */ jsx(Label, {
			className: "text-xs uppercase",
			children: label
		}), /* @__PURE__ */ jsx(Input, {
			className: "h-11",
			inputMode: numeric ? "decimal" : "text",
			value,
			onChange: (e) => onChange(e.target.value)
		})]
	});
}
//#endregion
export { SettingsPage as component };
