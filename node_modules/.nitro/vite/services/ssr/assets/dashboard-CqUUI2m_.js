import { A as prettyDate, D as inr, E as businessToday, M as shortDate, O as kg, S as useSettings, T as addDays, h as usePeriod, k as pct } from "./data-BXdCoojx.js";
import { r as Stat, t as DateBar } from "./Stat-C4Ts9jut.js";
import { t as Button } from "./button-BkEeRci-.js";
import { n as stockByType, r as summarize, t as change } from "./finance-3gIK0WeS.js";
import { useMemo, useState } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { AlertTriangle, CheckCircle2, Plus } from "lucide-react";
//#region src/routes/_authenticated/dashboard.tsx?tsr-split=component
function Dashboard() {
	const [date, setDate] = useState(businessToday());
	const day = usePeriod(date, date);
	const prevDay = usePeriod(addDays(date, -1), addDays(date, -1));
	const month = usePeriod(addDays(date, -29), date);
	const { data: settings } = useSettings();
	const s = summarize(day.data);
	const p = summarize(prevDay.data);
	const m = summarize(month.data);
	const stock = stockByType(month.data);
	const trend = useMemo(() => {
		const byDate = /* @__PURE__ */ new Map();
		for (let i = 29; i >= 0; i--) byDate.set(addDays(date, -i), {
			revenue: 0,
			weight: 0
		});
		for (const sale of month.data?.sales ?? []) {
			if (sale.status !== "active") continue;
			const row = byDate.get(sale.business_date);
			if (!row) continue;
			row.revenue += Number(sale.amount);
			row.weight += Number(sale.weight_kg);
		}
		return [...byDate.entries()].map(([d, v]) => ({
			day: shortDate(d),
			...v
		}));
	}, [month.data, date]);
	const tolerance = Number(settings?.stock_tolerance_kg ?? 1);
	const alerts = [];
	if (s.wastageKg > 0 && s.weightSold > 0 && s.wastageKg / s.weightSold * 100 > 2) alerts.push({
		tone: "warn",
		text: `Wastage is ${pct(s.wastageKg / s.weightSold * 100)} of chicken sold today.`
	});
	if (s.avgPurchaseRate > 0 && s.avgSellRate > 0 && s.avgSellRate - s.avgPurchaseRate < 15) alerts.push({
		tone: "warn",
		text: "Selling rate is close to purchase cost — low margin."
	});
	for (const row of stock) if (Math.abs(row.expected) > 0 && row.expected < tolerance) alerts.push({
		tone: "warn",
		text: `${row.name}: expected stock is low (${kg(row.expected, 1)}).`
	});
	if (alerts.length === 0 && s.saleCount > 0) alerts.push({
		tone: "ok",
		text: "No issues detected for the selected day."
	});
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsxs("div", {
			className: "mb-5 flex flex-wrap items-end justify-between gap-3",
			children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h1", {
				className: "text-2xl font-bold tracking-tight",
				children: "Brothers Chicken Adda"
			}), /* @__PURE__ */ jsx("p", {
				className: "mt-1 text-sm text-muted-foreground",
				children: prettyDate(date)
			})] }), /* @__PURE__ */ jsxs("div", {
				className: "flex gap-2",
				children: [/* @__PURE__ */ jsx(DateBar, {
					date,
					onChange: setDate
				}), /* @__PURE__ */ jsx(Button, {
					asChild: true,
					className: "h-11",
					children: /* @__PURE__ */ jsxs(Link, {
						to: "/sales",
						children: [/* @__PURE__ */ jsx(Plus, { className: "size-4" }), " Add sale"]
					})
				})]
			})]
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "grid grid-cols-2 gap-3 lg:grid-cols-4",
			children: [
				/* @__PURE__ */ jsx(Stat, {
					label: "Revenue",
					value: inr(s.revenue),
					sub: `${change(s.revenue, p.revenue).toFixed(0)}% vs yesterday`
				}),
				/* @__PURE__ */ jsx(Stat, {
					label: "Chicken sold",
					value: kg(s.weightSold, 2),
					sub: `${s.saleCount} sales`
				}),
				/* @__PURE__ */ jsx(Stat, {
					label: "Gross profit",
					value: inr(s.grossProfit),
					tone: s.grossProfit >= 0 ? "positive" : "negative",
					sub: "Revenue − chicken cost − direct costs"
				}),
				/* @__PURE__ */ jsx(Stat, {
					label: "Net profit",
					value: inr(s.netProfit),
					tone: s.netProfit >= 0 ? "positive" : "negative",
					sub: `${inr(s.netProfitPerKg, true)}/kg`
				})
			]
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "mt-3 grid grid-cols-2 gap-3 lg:grid-cols-4",
			children: [
				/* @__PURE__ */ jsx(Stat, {
					label: "Cash",
					value: inr(s.cash)
				}),
				/* @__PURE__ */ jsx(Stat, {
					label: "Online",
					value: inr(s.online)
				}),
				/* @__PURE__ */ jsx(Stat, {
					label: "Expenses",
					value: inr(s.expensesTotal),
					tone: "caution"
				}),
				/* @__PURE__ */ jsx(Stat, {
					label: "Wastage",
					value: kg(s.wastageKg, 2),
					sub: inr(s.wastageCost),
					tone: "caution"
				})
			]
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "mt-6 grid gap-4 lg:grid-cols-3",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "rounded-xl border bg-card p-4 shadow-[var(--shadow-card)] lg:col-span-2",
				children: [/* @__PURE__ */ jsx("h2", {
					className: "text-sm font-semibold",
					children: "Revenue — last 30 days"
				}), /* @__PURE__ */ jsx("div", {
					className: "mt-3 h-56",
					children: /* @__PURE__ */ jsx(ResponsiveContainer, {
						width: "100%",
						height: "100%",
						children: /* @__PURE__ */ jsxs(AreaChart, {
							data: trend,
							margin: {
								left: -18,
								right: 8,
								top: 8
							},
							children: [
								/* @__PURE__ */ jsx("defs", { children: /* @__PURE__ */ jsxs("linearGradient", {
									id: "rev",
									x1: "0",
									y1: "0",
									x2: "0",
									y2: "1",
									children: [/* @__PURE__ */ jsx("stop", {
										offset: "0%",
										stopColor: "var(--color-primary)",
										stopOpacity: .35
									}), /* @__PURE__ */ jsx("stop", {
										offset: "100%",
										stopColor: "var(--color-primary)",
										stopOpacity: 0
									})]
								}) }),
								/* @__PURE__ */ jsx(CartesianGrid, {
									strokeDasharray: "3 3",
									stroke: "var(--color-border)"
								}),
								/* @__PURE__ */ jsx(XAxis, {
									dataKey: "day",
									tick: { fontSize: 11 },
									interval: 4
								}),
								/* @__PURE__ */ jsx(YAxis, {
									tick: { fontSize: 11 },
									width: 54
								}),
								/* @__PURE__ */ jsx(Tooltip, {
									formatter: (v) => inr(v),
									contentStyle: {
										borderRadius: 10,
										border: "1px solid var(--color-border)",
										background: "var(--color-card)"
									}
								}),
								/* @__PURE__ */ jsx(Area, {
									type: "monotone",
									dataKey: "revenue",
									stroke: "var(--color-primary)",
									fill: "url(#rev)",
									strokeWidth: 2
								})
							]
						})
					})
				})]
			}), /* @__PURE__ */ jsxs("div", {
				className: "space-y-4",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "rounded-xl border bg-card p-4 shadow-[var(--shadow-card)]",
					children: [/* @__PURE__ */ jsx("h2", {
						className: "text-sm font-semibold",
						children: "Current stock (30-day flow)"
					}), /* @__PURE__ */ jsx("div", {
						className: "mt-3 space-y-2",
						children: stock.length === 0 ? /* @__PURE__ */ jsx("p", {
							className: "text-sm text-muted-foreground",
							children: "Record a purchase to start tracking stock."
						}) : stock.map((row) => /* @__PURE__ */ jsxs("div", {
							className: "flex items-center justify-between text-sm",
							children: [/* @__PURE__ */ jsx("span", { children: row.name }), /* @__PURE__ */ jsx("span", {
								className: "num font-semibold",
								children: kg(row.expected, 1)
							})]
						}, row.name))
					})]
				}), /* @__PURE__ */ jsxs("div", {
					className: "rounded-xl border bg-card p-4 shadow-[var(--shadow-card)]",
					children: [/* @__PURE__ */ jsx("h2", {
						className: "text-sm font-semibold",
						children: "Business alerts"
					}), /* @__PURE__ */ jsx("ul", {
						className: "mt-3 space-y-2 text-sm",
						children: alerts.length === 0 ? /* @__PURE__ */ jsx("li", {
							className: "text-muted-foreground",
							children: "No data yet for this day."
						}) : alerts.map((a, i) => /* @__PURE__ */ jsxs("li", {
							className: "flex items-start gap-2",
							children: [a.tone === "warn" ? /* @__PURE__ */ jsx(AlertTriangle, { className: "mt-0.5 size-4 shrink-0 text-caution" }) : /* @__PURE__ */ jsx(CheckCircle2, { className: "mt-0.5 size-4 shrink-0 text-positive" }), /* @__PURE__ */ jsx("span", { children: a.text })]
						}, i))
					})]
				})]
			})]
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "mt-6 rounded-xl border bg-card p-4 shadow-[var(--shadow-card)]",
			children: [/* @__PURE__ */ jsx("h2", {
				className: "text-sm font-semibold",
				children: "Last 30 days"
			}), /* @__PURE__ */ jsxs("div", {
				className: "mt-3 grid grid-cols-2 gap-3 lg:grid-cols-4",
				children: [
					/* @__PURE__ */ jsx(Stat, {
						label: "Revenue",
						value: inr(m.revenue)
					}),
					/* @__PURE__ */ jsx(Stat, {
						label: "Avg selling rate",
						value: `${inr(m.avgSellRate, true)}/kg`
					}),
					/* @__PURE__ */ jsx(Stat, {
						label: "Avg purchase cost",
						value: `${inr(m.avgPurchaseRate, true)}/kg`
					}),
					/* @__PURE__ */ jsx(Stat, {
						label: "Net profit",
						value: inr(m.netProfit),
						tone: m.netProfit >= 0 ? "positive" : "negative"
					})
				]
			})]
		})
	] });
}
//#endregion
export { Dashboard as component };
